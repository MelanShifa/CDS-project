https://cs-project.gitbook.io/python-script-documentation/

The following link leads to a gitbook documentation of how the parser works.

Note: The "DataParser.ipynb" and "DataParser.py" files are both same but in different formats. The .ipynb format makes it easier to debug and add functions, so we decided to keep it for the next team.
